import { Box } from '@mui/material';
import { ContainerSideBar } from './style';

const SideBar: React.FC = () => {
  return (
    <ContainerSideBar>
      <div>Boa</div>
    </ContainerSideBar>
  );
};

export { SideBar };
